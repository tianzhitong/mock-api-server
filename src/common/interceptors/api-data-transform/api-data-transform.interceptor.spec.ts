import { ApiDataTransformInterceptor } from './api-data-transform.interceptor';

describe('ApiDataTransformInterceptor', () => {
  it('should be defined', () => {
    expect(new ApiDataTransformInterceptor()).toBeDefined();
  });
});
